# Руководство по развертыванию модернизированного модуля "Новости"

## 🚀 Обзор изменений

Модуль "Новости" был полностью модернизирован с сохранением обратной совместимости. Добавлены новые возможности:

- ✅ Современный UI с Tailwind CSS
- ✅ Drag & Drop загрузка медиа
- ✅ Поддержка фото и видео в едином слайдере
- ✅ Автокомплит тегов и категорий
- ✅ Планировщик публикации
- ✅ Сортировка медиа перетаскиванием
- ✅ Выбор обложки из медиа
- ✅ Расширенные фильтры и поиск

## 📋 Предварительные требования

- Laravel 10/11
- PHP 8.1+
- Node.js 16+
- MySQL/MariaDB
- Composer
- NPM/Yarn

## 🔧 Установка

### 1. Обновление зависимостей

```bash
# Backend зависимости
composer install

# Frontend зависимости
npm install
```

### 2. Настройка файловой системы

```bash
# Создание симлинка для storage
php artisan storage:link

# Проверка прав доступа
chmod -R 755 storage/app/public
chmod -R 755 public/storage
```

### 3. Миграции базы данных

```bash
# Применение миграций (если есть новые)
php artisan migrate

# Запуск сидеров (опционально)
php artisan db:seed --class=NewsSeeder
```

### 4. Сборка фронтенда

```bash
# Разработка
npm run dev

# Продакшн
npm run build
```

## 📁 Новые файлы

### Backend
```
app/
├── Services/MediaService.php (новый)
├── Http/Requests/NewsRequest.php (новый)
└── Http/Controllers/Admin/NewsController.php (обновлен)
```

### Frontend
```
resources/js/
├── Components/Admin/News/
│   ├── ModernMediaUploader.jsx (новый)
│   ├── TagSelector.jsx (новый)
│   ├── CategorySelector.jsx (новый)
│   ├── StatusBadge.jsx (новый)
│   └── DateTimePicker.jsx (новый)
└── Pages/Admin/News/
    ├── CreateModern.jsx (новый)
    └── IndexModern.jsx (новый)
```

### Тесты
```
tests/Feature/NewsManagementTest.php (новый)
```

## 🔄 Обратная совместимость

### ✅ Что сохраняется:
- Все существующие поля БД
- API endpoints (расширены, но не изменены)
- Существующие новости продолжают работать
- Старые медиа файлы поддерживаются

### 🔧 Что изменилось:
- Добавлены новые API endpoints для медиа
- Расширена валидация
- Улучшен UI/UX
- Добавлена поддержка тегов

## 🛠️ Конфигурация

### 1. Переменные окружения

Убедитесь, что в `.env` установлены:

```env
FILESYSTEM_DISK=public
APP_URL=http://your-domain.com
```

### 2. Права доступа

```bash
# Права на storage
chown -R www-data:www-data storage/
chmod -R 755 storage/

# Права на public/storage
chown -R www-data:www-data public/storage/
chmod -R 755 public/storage/
```

### 3. Веб-сервер (Apache)

Убедитесь, что `.htaccess` в `public/storage/` содержит:

```apache
<FilesMatch "\.(jpg|jpeg|png|gif|webp|svg|ico|mp4|avi|mov|wmv|flv|webm|ogg)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

<FilesMatch "\.(mp4|avi|mov|wmv|flv|webm|ogg)$">
    Header set Cache-Control "public, max-age=31536000"
    Header set Access-Control-Allow-Origin "*"
    Header set Accept-Ranges "bytes"
</FilesMatch>
```

## 🧪 Тестирование

### 1. Запуск тестов

```bash
# Все тесты
php artisan test

# Только тесты новостей
php artisan test tests/Feature/NewsManagementTest.php

# С покрытием
php artisan test --coverage
```

### 2. Ручное тестирование

1. **Создание новости:**
   - Перейдите в админку → Новости → Создать
   - Заполните форму
   - Загрузите медиа файлы
   - Сохраните

2. **Загрузка медиа:**
   - Drag & Drop файлов
   - Проверьте предпросмотр
   - Установите обложку
   - Измените порядок

3. **Планировщик:**
   - Создайте новость со статусом "Запланировано"
   - Установите дату в будущем
   - Проверьте, что новость не видна на сайте

## 🚨 Устранение неполадок

### Проблема: Медиа файлы не загружаются

**Решение:**
```bash
# Проверьте симлинк
ls -la public/storage

# Пересоздайте симлинк
rm public/storage
php artisan storage:link

# Проверьте права
chmod -R 755 storage/app/public
```

### Проблема: 403 ошибка при доступе к медиа

**Решение:**
1. Проверьте `.htaccess` в `public/storage/`
2. Убедитесь, что веб-сервер имеет права на чтение файлов
3. Проверьте настройки CORS

### Проблема: JavaScript ошибки

**Решение:**
```bash
# Пересоберите фронтенд
npm run build

# Очистите кэш
php artisan cache:clear
php artisan view:clear
```

### Проблема: Валидация не работает

**Решение:**
1. Проверьте, что `NewsRequest` зарегистрирован
2. Убедитесь, что CSRF токен передается
3. Проверьте логи Laravel

## 📊 Мониторинг

### 1. Логи

Проверяйте логи для отслеживания ошибок:

```bash
tail -f storage/logs/laravel.log
```

### 2. Производительность

- Мониторьте размер директории `storage/app/public/news/`
- Настройте очистку старых медиа файлов
- Используйте CDN для больших файлов

### 3. Безопасность

- Регулярно обновляйте зависимости
- Проверяйте права доступа к файлам
- Мониторьте загрузку подозрительных файлов

## 🔄 Обновление

### Следующие версии

При обновлении:

1. Сделайте бэкап БД
2. Обновите зависимости
3. Запустите миграции
4. Пересоберите фронтенд
5. Протестируйте функциональность

### Откат изменений

Если нужно откатиться:

1. Восстановите бэкап БД
2. Откатите код к предыдущей версии
3. Пересоберите фронтенд
4. Очистите кэш

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи Laravel
2. Убедитесь в правильности конфигурации
3. Проверьте права доступа
4. Обратитесь к документации Laravel

## ✅ Чек-лист развертывания

- [ ] Обновлены зависимости
- [ ] Создан симлинк storage
- [ ] Настроены права доступа
- [ ] Обновлен .htaccess
- [ ] Собран фронтенд
- [ ] Запущены тесты
- [ ] Проверена загрузка медиа
- [ ] Протестированы все функции
- [ ] Настроен мониторинг
- [ ] Создан бэкап

---

**Готово!** Модернизированный модуль "Новости" готов к использованию.
