# 🌐 Инструкция по настройке системы переводов

## Обзор системы

Система переводов работает по **умной схеме**, которая оптимизирует использование Google Translate API:

1. **Сначала проверяет базу данных** - если текст уже переводился, использует сохраненный перевод
2. **Переводит через Google Translate** - только новые, непереведенные тексты
3. **Сохраняет в БД** - все переводы сохраняются для повторного использования
4. **Кеширует результаты** - для максимальной производительности

**Преимущества:**
- ⚡ Быстрая работа за счет переиспользования переводов из БД
- 💰 Экономия средств на API запросах
- 🔄 Автоматическое обновление при добавлении нового контента
- 🛡️ Защита специальных терминов при переводе

---

## Настройка Google Translate API

### Шаг 1: Получение API ключа

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект или выберите существующий
3. Включите **Cloud Translation API**:
   - Перейдите в "APIs & Services" → "Library"
   - Найдите "Cloud Translation API"
   - Нажмите "Enable"
4. Создайте API ключ:
   - Перейдите в "APIs & Services" → "Credentials"
   - Нажмите "Create Credentials" → "API Key"
   - Скопируйте полученный ключ

### Шаг 2: Настройка в Laravel

1. Откройте файл `.env` в корне проекта
2. Добавьте или обновите строку:

```env
GOOGLE_TRANSLATE_API_KEY=ваш_api_ключ_здесь
```

3. Сохраните файл
4. Очистите кеш конфигурации:

```bash
php artisan config:clear
php artisan cache:clear
```

---

## Использование админки переводов

### Доступ к панели управления

Перейдите по адресу: `http://127.0.0.1/admin/translations`

### Массовый перевод

1. Выберите целевой язык (Қазақша или English)
2. Нажмите кнопку **"Запустить массовый перевод"**
3. Система автоматически:
   - Проверит все тексты в БД
   - Найдет непереведенные тексты
   - Проверит наличие готовых переводов в БД
   - Переведет новые тексты через Google Translate
   - Сохранит результаты

### Статистика

После завершения перевода вы увидите детальную статистику:
- **Всего обработано** - количество проверенных текстов
- **Успешно переведено** - количество успешных переводов
- **Из базы данных** - сколько переводов было взято из БД (экономия API)
- **Через Google Translate** - сколько новых переводов создано
- **Ошибок** - количество неудачных переводов

---

## API эндпоинты

### Массовый перевод

**POST** `/admin/translations/translate-all`

```json
{
  "target_language": "kk"  // или "en"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Переведено: 150 (из БД: 80, через Google: 70), ошибок: 0",
  "stats": {
    "translated": 150,
    "from_db": 80,
    "from_google": 70,
    "failed": 0,
    "total": 150
  }
}
```

### Перевод отдельного текста

**POST** `/admin/translations/translate-single`

```json
{
  "text": "Текст для перевода",
  "target_language": "kk",
  "scope": "manual",  // опционально
  "key": "unique_key"  // опционально
}
```

**Ответ:**
```json
{
  "success": true,
  "translation": "Аударуға арналған мәтін",
  "source": "database",  // или "google_translate"
  "message": "Перевод найден в базе данных"
}
```

---

## Структура базы данных

Таблица `translations`:

| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Первичный ключ |
| scope | varchar(120) | Область перевода (ui, content, news) |
| key | varchar(190) | Уникальный ключ перевода |
| ru | text | Русский текст (исходный) |
| kk | text | Казахский перевод |
| en | text | Английский перевод |
| hash | varchar(64) | SHA-256 хеш русского текста |
| meta | json | Дополнительные метаданные |
| updated_by | bigint | ID пользователя, обновившего перевод |
| created_at | timestamp | Дата создания |
| updated_at | timestamp | Дата обновления |

**Индексы:**
- `scope + key` - уникальный составной ключ
- `hash` - для быстрого поиска по содержимому
- `updated_at` - для сортировки по дате

---

## Логика работы

### При массовом переводе

```
Для каждого непереведенного текста:
  1. Ищем в БД запись с таким же RU текстом и заполненным целевым языком
     ↓
  2. Найден?
     ├─ ДА → Копируем перевод из найденной записи
     └─ НЕТ → Переводим через Google Translate API
                ↓
                Сохраняем новый перевод в БД
  3. Очищаем кеш
```

### При работе сайта

```
Запрос перевода:
  1. Проверяем кеш
     ↓
  2. Есть в кеше?
     ├─ ДА → Возвращаем из кеша
     └─ НЕТ → Читаем из БД
                ↓
                Сохраняем в кеш
                ↓
                Возвращаем результат
```

---

## Оптимизация и рекомендации

### Производительность

1. **Кеширование** - переводы кешируются на 24 часа (по умолчанию)
2. **Индексы БД** - используйте составные индексы для быстрого поиска
3. **Очередь переводов** - большие объемы лучше переводить через очередь

### Экономия API запросов

1. **Повторное использование** - система автоматически находит уже переведенные тексты
2. **Массовый перевод** - переводите сразу большие объемы контента
3. **Проверка перед переводом** - используйте метод `translateSingle` для разовых переводов

### Безопасность

1. **API ключ** - храните ключ только в `.env`, не комитьте в Git
2. **Авторизация** - доступ к админке только для авторизованных пользователей
3. **Логирование** - все переводы логируются для аудита

---

## Устранение проблем

### Google Translate API не работает

**Проблема:** "Google Translate API key is not configured"

**Решение:**
1. Проверьте наличие ключа в `.env`
2. Убедитесь, что API включен в Google Cloud Console
3. Очистите кеш: `php artisan config:clear`

### Медленные переводы

**Проблема:** Массовый перевод занимает много времени

**Решение:**
1. Используйте очередь Laravel: `php artisan queue:work`
2. Увеличьте timeout в `GoogleTranslateProvider`
3. Переводите меньшими партиями

### Неправильные переводы

**Проблема:** Качество переводов низкое

**Решение:**
1. Добавьте термины в глоссарий для защиты специальных слов
2. Проверьте и отредактируйте переводы вручную в админке
3. Используйте метод `retranslate` для повторного перевода

---

## Дополнительные возможности

### Защита терминов

Система автоматически защищает специальные термины из глоссария при переводе.

### Экспорт переводов

Экспортируйте словарь переводов в JSON:
- GET `/admin/translations/export?scope=ui&locale=kk`

### Очистка кеша

Очистите кеш переводов:
- POST `/admin/translations/clear-cache`

---

## Контакты и поддержка

При возникновении вопросов обращайтесь к документации Laravel или в службу поддержки проекта.

**Версия:** 1.0  
**Дата обновления:** 31 октября 2024

